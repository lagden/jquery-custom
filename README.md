# jquery-custom

My own custom build of jQuery.

    grunt custom:
      +ajax
      +dimensions
      +event
      +offset
      +deferred
      +exports/amd
      +sizzle
      -deprecated
      -effects
      -wrap
      -core/ready
      -exports/global

